// Filename: /frontend/app/about/developer/page.tsx
// Arc Codex Infrastructure — The Architecture of Intent
// v7.0 Apr 20 2026 — Refined for Epistemic Responsibility

import React from 'react';
import PageWrapper from '@/components/layout/PageWrapper';

const TechBlock = ({ label, value, color }: { label: string, value: string, color: string }) => (
  <div className="border-b border-slate-200 py-4 group">
    <dt className="text-[10px] font-mono uppercase tracking-[0.3em] text-slate-400 group-hover:text-slate-900 transition-colors">
      {label}
    </dt>
    <dd className={`text-sm font-mono mt-1 ${color} break-words`}>
      {value}
    </dd>
  </div>
);

export default function DeveloperCodex() {
  return (
    <PageWrapper>
      <div className="min-h-screen bg-[#fdfbf7] text-slate-900 p-8 md:p-24 font-ubuntu">
        <main className="max-w-5xl mx-auto space-y-32">
          
          {/* HEADER: THE BLUEPRINT */}
          <header className="space-y-6">
            <h1 className="text-5xl font-bold tracking-tighter italic">The Infrastructure of Meaning.</h1>
            <p className="text-sm font-mono uppercase tracking-[0.5em] text-slate-400 border-t border-slate-200 pt-4">
              System Architecture // Stack v6.8 // Fort Collins, CO
            </p>
          </header>

          {/* SECTION I: THE ENGINE ROOM */}
          <section className="grid md:grid-cols-2 gap-16">
            <div className="space-y-8">
              <h2 className="text-2xl font-bold italic border-l-4 border-slate-900 pl-4">I. The Foundation</h2>
              <p className="text-slate-600 leading-relaxed italic">
                "A system is only as honest as its lowest layer."
              </p>
              <dl className="space-y-2">
                <TechBlock label="Intelligence API" value="Flask / Gunicorn :5005" color="text-emerald-700" />
                <TechBlock label="Reactive Interface" value="Next.js 16.1.6 / React 19" color="text-blue-700" />
                <TechBlock label="The Memory" value="Redis 6379 (DB 0)" color="text-purple-700" />
                <TechBlock label="The Archive" value="Apache Solr 8983" color="text-cyan-700" />
                <TechBlock label="Inference" value="Ollama (M1 Air @ 192.168.1.185)" color="text-orange-700" />
              </dl>
            </div>

            <div className="bg-slate-900 text-slate-300 p-8 rounded-sm shadow-2xl font-mono text-xs leading-relaxed">
              <div className="text-emerald-400 mb-4 tracking-widest uppercase"># arc.sh control_logic</div>
              <p className="opacity-50">./arc.sh status // Check heartbeats</p>
              <p className="opacity-50">./arc.sh backup // SSD snapshot (last 5)</p>
              <p className="text-white mt-4 italic font-ubuntu text-base">
                "The watchdog restarts the fallen, but only if the PID remains as a ghost of intent."
              </p>
              <div className="mt-8 pt-4 border-t border-slate-800 text-[10px] uppercase">
                Watchdog Loop: 60s
              </div>
            </div>
          </section>

          {/* SECTION II: THE PIPELINE */}
          <section className="space-y-12">
            <div className="max-w-2xl">
              <h2 className="text-3xl font-bold tracking-tight">II. The Scribe's Sequence</h2>
              <p className="mt-4 text-slate-600 leading-relaxed">
                Every 13 minutes, the <strong>Scribe</strong> awakens. It is not merely a scraper; 
                it is a harvester of signal. It moves from RSS ingestion to NLP refinement, 
                dispatching raw text into the A.R.C. triad for stabilization.
              </p>
            </div>
            
            <div className="grid md:grid-cols-3 gap-4">
              {[
                { step: "01", name: "Ingestion", desc: "2,088 RSS Streams" },
                { step: "02", name: "Analysis", desc: "Red/Blue/Purple Triad" },
                { step: "03", name: "Publication", desc: "Solr/Redis Persistence" },
              ].map((item) => (
                <div key={item.step} className="border border-slate-200 p-6 rounded-sm bg-white hover:bg-slate-50 transition-colors">
                  <span className="text-[10px] font-mono text-slate-400">{item.step}</span>
                  <h3 className="text-lg font-bold mt-1 italic">{item.name}</h3>
                  <p className="text-xs text-slate-500 mt-2 font-mono uppercase tracking-tighter">{item.desc}</p>
                </div>
              ))}
            </div>
          </section>

          {/* FOOTER: THE STEWARDSHIP */}
          <footer className="pt-24 border-t border-slate-200 flex flex-col md:flex-row justify-between items-center gap-8">
            <div className="text-[10px] font-mono uppercase tracking-[0.4em] text-slate-400">
              Harold Edwin Ross Nesbitt III // 30 Years of Infrastructure
            </div>
            <div className="flex gap-4">
               <a href="https://github.com/hapnesbitt/arc-codex" className="text-xs font-mono border-b border-slate-900 pb-1">GIT_SOURCE</a>
               <a href="/about/vision" className="text-xs font-mono border-b border-slate-900 pb-1">VISION_DOCTRINE</a>
            </div>
          </footer>

        </main>
      </div>
    </PageWrapper>
  );
}
